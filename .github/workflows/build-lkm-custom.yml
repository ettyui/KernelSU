name: Build LKM for KernelSU (Custom)
on:
  workflow_call:
  workflow_dispatch:
jobs:
  build-lkm:
    strategy:
      matrix:
        kmi:
          - android15-6.6   # 只保留你需要的
    uses: ./.github/workflows/ddk-lkm.yml
    with:
      kmi: ${{ matrix.kmi }}
      ddk_release: '20251104'